

host: Option<[u8; 4]>,
        

pub struct Peer {
    //inner: Rc<RefCell<NodeInner>>,
    //pub timer: Timer,
    host: Option<[u8; 4]>,
    port: Option<u16>,
}

impl Peer {
    pub fn new(addr: SocketAddr) -> Node {

    }
}
        